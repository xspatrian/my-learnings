thislist = ["apple", "banana", "cherry"]  # Initialize the list
for i in range(len(thislist)):            # Iterate over the indices of the list
    print(thislist[i])                    # Print the element at the current index
